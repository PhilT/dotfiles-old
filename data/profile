#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
	. "$HOME/.bashrc"
fi

PATH="$HOME/bin:$PATH"

export EDITOR=vim
export CDPATH=/home/phil/ws
PATH=.bin/safe/../../.bin:$HOME/.rbenv/bin:$HOME/.rbenv/shims:$PATH

if [[ $(which startx &>-) && ! $(pgrep startx &>-) ]]; then
  echo Starting X...
  startx
fi
